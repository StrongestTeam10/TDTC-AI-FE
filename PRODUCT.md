# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

권한 코드와 소속 코드는 DB(`comcode01m`)가 유일한 출처이며 `useCommonCodes`로만 조회한다.

| 코드 | 역할 | 주요 진입 화면 |
|---|---|---|
| `ROL01` | 관리자 | 전 화면 + 회원 승인(`/admin/users`) |
| `ROL02` | 관제요원 | 관제 대시보드(`/dashboard`), 시뮬레이션 비교(`/compare`) |
| `ROL03` | 조회자 | 게시판(`/board/**`) |

소속: `ORGKT` KT · `ORGGV` 지자체 · `ORGMA` 상인회. 시장 구조 등록(`/facilities`)은 `ROL01` 또는 `ORGMA`.

**주 사용자는 관제요원(`ROL02`)이다.** 관제실에서 실시간 CCTV 인파 상황을 지켜보다가, "이 통로를 막으면 대피 시간이 어떻게 달라지는가"를 시뮬레이션으로 확인하고, 그 근거를 정책 보고서로 반출하는 일을 한다.

가입 즉시 접근은 불가능하다 — 관제 데이터를 다루므로 관리자 승인 워크플로우가 선행한다.

## Product Purpose

전통시장 AI 안전탐지 관제 솔루션의 프론트엔드. CCTV로 측정한 실제 인파를 디지털 트윈 시뮬레이션에 얹어 개입 전/후를 나란히 비교하고, 근거가 붙은 정책 보고서(DOCX)를 자동 생성한다.

성공 = 관제요원이 개입 시나리오의 효과를 수치로 확인하고, 그 근거를 문서로 반출할 수 있는 것.

## Positioning

인파 감지에서 끝나지 않고 **"측정 → 시뮬레이션 → 정책 근거 문서"까지 한 화면에서 연결한다.** `/compare`가 그 핵심으로, 개입 전(`predict`)과 개입 후(`run`)를 동시에 실행해 듀얼 맵으로 나란히 세운다. 단순 CCTV 관제 제품이 그대로 따라 할 수 없는 지점은 "개입의 효과를 사전에 수치로 보여준다"는 것이다.

## Operating Context

- **뷰포트**: 데스크톱 브라우저(1280~1920px) + 관제실 대형 모니터(2560px+). 모바일·태블릿은 정식 요구사항이 아니다.
- **FE는 서버 두 곳을 직접 호출한다.** Java BE(JWT 자동 첨부, `src/api/client.ts`)와 CCTV AI(무인증, `src/api/cctvClient.ts`). 시뮬레이션 엔진(SIM)은 BE를 거쳐서만 닿는다. CCTV AI에 JWT를 붙이면 안 된다.
- **긴 대기가 정상 동작이다.** 시뮬레이션 타임아웃 5분(공용 15초와 별도), 정책 보고서 생성 1~3분.
- `/compare`는 두 요청을 `Promise.allSettled`로 동시에 보내고, 한쪽이 실패해도 성공한 쪽은 그린다.
- **배포**: AWS CodePipeline이 유일 경로 (GitHub push → CodeBuild → S3 → CloudFront). `VITE_*`는 빌드 시점에 번들로 들어가므로 CodeBuild 환경변수에 등록하며 소스에 커밋하지 않는다.
- CCTV AI는 **8088**이다. 8000은 SIM이 쓴다.

## Capabilities and Constraints

- **권한 판정은 `src/auth/permissions.ts` 한 곳에서만 한다.** 헤더 메뉴 노출과 라우트 가드가 같은 함수를 쓴다. `homePathFor`는 그 사용자가 실제로 들어갈 수 있는 경로만 돌려줘야 한다 — 가드 3개가 모두 이 함수로 되돌려 보내므로 못 가는 경로를 주면 무한 리다이렉트가 난다.
- 화면 가드는 노출 정리 용도이고 **최종 차단은 BE**가 한다(`SecurityConfig`).
- 공통코드(`ROL` · `ORG` · `LVL` · `POL` · `MKT` · `BCT`)는 DB가 유일한 출처다. `constants/*.ts`의 상수는 네트워크 실패 시 폴백일 뿐이며, 화면에 코드 표를 만들어 두지 않는다.
- `src/types/index.ts`의 필드명은 Spring Boot DTO와 정확히 일치해야 한다. 어긋나면 컴파일은 통과하고 런타임에 `undefined`로 조용히 깨진다.
- **다크 모드는 Tailwind v4 `.dark` 클래스 기반**이다(`src/index.css`의 `@custom-variant`). 새 색을 넣을 때 `dark:` 짝을 같이 넣어야 한다.
- **지도가 두 종류인 것은 의도다.** `HeatmapView`는 수백 명의 에이전트를 매 프레임 다시 그려야 해서 SVG 직접 렌더이고, `FacilityLocationPicker`는 실제 지번·건물을 봐야 해서 카카오맵이다. 카카오 JS 키는 도메인 제한이 필수다.
- `npm run build`는 `tsc -b`를 먼저 돌려 타입 오류가 하나라도 있으면 실패한다.
- **일정 제약**: 프로젝트 종료일 **2026-08-21**. 현재는 신규 기능이 아니라 기존 기능의 품질을 다듬는 단계다. 이 시점 이후를 전제한 대규모 리팩터링·리디자인은 범위 밖이다.

## Evidence on Hand

실물로 존재하는 것:

- `README.md` — 아키텍처 상 위치, 화면·권한 표, 개발 시 주의사항. `CHANGELOG.md` — 변경 이력.
- `C:\workspace\PORTFOLIO-TDTC.md` — 시스템 아키텍처와 기술적 의사결정 기록.
- `src/data/realFrameData.ts` — 실측 프레임 데이터.
- `src/components/legal/` + `src/constants/legalText.ts` — 이용약관·개인정보처리방침 실제 문안.
- 실제 시장 데이터가 공통코드 `MKT` 도메인에 있다(예: `MKTMW` 망원시장).

**없는 것 — 미래 작업이 지어내면 안 된다**: 사용자 리서치 기록, 실사용 로그·지표, 성능 측정 baseline, 고객 사례·추천사, 가격·라이선스 정보.

## Product Principles

1. **권한 판정은 한 곳에서.** 헤더 노출과 라우트 가드가 같은 함수를 쓴다. 새 화면을 더할 때도 조건을 복제하지 않는다.
2. **코드값은 DB가 정답.** 화면에 표를 만들지 않는다.
3. **긴 대기를 감추지 않고 설명한다.** 시뮬레이션 5분·보고서 1~3분은 정상이다. 무슨 일이 진행 중인지 보여주는 것이 UI의 일이다.
4. **한쪽이 실패해도 성공한 쪽은 보여준다.** `/compare`의 `allSettled`가 이 원칙의 구현이다. 부분 실패를 전체 실패로 만들지 않는다.
5. **한계를 감추지 않는다.** 제약과 그로 인해 남은 위험을 명시하는 것이 이 팀의 방식이다(포트폴리오 회고에 명시). UI도 같아서, 데이터가 없거나 신뢰도가 낮으면 그렇다고 말한다.

## Accessibility & Inclusion

**KWCAG 2.2(한국형 웹 콘텐츠 접근성 지침) 준수가 요구된다.** 지자체 대상 공공 조달 기준이 걸려 있으므로 위반은 P0/P1로 취급한다. WCAG 2.1 AA는 그 하위 집합으로 함께 충족되어야 한다 — 대비 4.5:1, 키보드 내비게이션, 폼 레이블, 초점 표시.

관제실 대형 모니터 사용을 전제하므로 **원거리 판독성**(작은 본문·낮은 대비의 보조 텍스트)도 접근성 문제로 본다.
